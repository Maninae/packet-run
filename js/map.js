// map.js — SVG region map: nodes, roads, hint glyphs, fog-of-detail.
// Responsibility: render the hand-authored Phase 1a map (MAP_1A in config.js)
// and the junction interaction (tap a road → highlight + glyphs → tap to commit;
// build card #9). Shape always visible; hazard glyphs only ~1 hop ahead.
// Threat glyphs are FORECASTS ("the storm is eyeing #2 and #4") — never prophecy
// (design/07-accuracy.md).

export function renderMap(state, svgRoot) {
  // Phase 1a implementation lands after the portrait mock is approved (Gate 1).
}
