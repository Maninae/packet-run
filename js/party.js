// party.js — Canvas live layer: Pip + fragments racing the wires.
// Responsibility: draw the party as visible bodies (Integrity = who's still
// with you), fragments scattering onto different wires and regrouping SHUFFLED
// at nodes — never an in-order train (design/07-accuracy.md) — plus juice
// (chimes, blips, the dock filling). Respect prefers-reduced-motion.

export function renderParty(state, canvas) {
  // Phase 1a implementation lands after the portrait mock is approved (Gate 1).
}
