{
  "name": "packet-run",
  "private": true,
  "description": "Dev tooling only — the game itself is static vanilla JS with no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/unit/",
    "test:e2e": "node --test tests/e2e/",
    "serve": "python3 -m http.server 8123"
  },
  "devDependencies": {
    "playwright": "1.58.2"
  }
}
